package neutrino.UserManager;

public class EffectNumber
{
	// all credits to matty13
     public static int GetEffectNum(Effects effect)
     {
    	 if (effect == Effects.NONE)
             return 0;
    	 
    	 if (effect == Effects.GLOW)
             return 1;

         if (effect == Effects.BLUE_SKATEBOARD)
             return 2;

         if (effect == Effects.BLUE_CAR)
             return 3;

         if (effect == Effects.SPARKLE)
             return 4;

         if (effect == Effects.TORCH)
             return 5;

         if (effect == Effects.ROCKET)
             return 6;

         if (effect == Effects.BUTTERFLIES)
             return 7;

         if (effect == Effects.FIREFLIES)
             return 8;

         if (effect == Effects.LOVE_HEARTS)
             return 9;

         if (effect == Effects.FLIES)
             return 10;

         if (effect == Effects.GREEN_GLOW)
             return 11;

         if (effect == Effects.FROZEN)
             return 12;

         if (effect == Effects.GHOST)
             return 13;

         if (effect == Effects.PINK_SKATEBOARD)
             return 14;

         if (effect == Effects.YELLOW_SKATEBOARD)
             return 15;

         if (effect == Effects.MICROPHONE)
             return 16;

         if (effect == Effects.PINK_CAR)
             return 17;

         if (effect == Effects.YELLOW_CAR)
             return 18;

         if (effect == Effects.POLICE_CAR)
             return 19;

         if (effect == Effects.AMBULANCE_CAR)
             return 20;

         if (effect == Effects.BLUE_SPORTS_CAR)
             return 21;

         if (effect == Effects.YELLOW_SPORTS_CAR)
             return 22;

         if (effect == Effects.FLOATING_GREEN_GLOW)
             return 23;

         if (effect == Effects.RAINING)
             return 24;

         if (effect == Effects.FIRE)
             return 25;

         if (effect == Effects.FIRE_STAFF)
             return 26;

         if (effect == Effects.KNIGHT_HELMET_WITH_SWORD)
             return 27;

         if (effect == Effects.SWIMMING_IN_WATER)
             return 28;

         if (effect == Effects.SWIMMING_IN_WATER2)
             return 29;

         if (effect == Effects.PADDLING_IN_WATER)
             return 30;

         if (effect == Effects.TIGER)
             return 31;

         if (effect == Effects.DESPICABLE_ME_MINION_EYE)
             return 32;

         if (effect == Effects.BANZAI_PURPLE)
             return 33;

         if (effect == Effects.BANZAI_GREEN)
             return 34;

         if (effect == Effects.BANZAI_BLUE)
             return 35;

         if (effect == Effects.BANZAI_YELLOW)
             return 36;

         if (effect == Effects.PADDLING_GREEN_WATER)
             return 37;

         if (effect == Effects.MALE_ICE_SKATES)
             return 38;

         if (effect == Effects.FEMALE_ICE_SKATES)
             return 39;

         if (effect == Effects.FREEZE_GAME_RED_HELMET)
             return 40;

         if (effect == Effects.FREEZE_GAME_GREEN_HELMET)
             return 41;

         if (effect == Effects.FREEZE_GAME_WHITE_HELMET)
             return 42;

         if (effect == Effects.FREEZE_GAME_BLACK_HELMET)
             return 43;

         if (effect == Effects.SIMS_DIAMOND)
             return 44;

         if (effect == Effects.FLOATING_SPARKLING_GLOW)
             return 45;

         if (effect == Effects.FLOATING_SPARKLING_GLOW2)
             return 46;

         if (effect == Effects.MEGAMIND_BLUE_FACE)
             return 47;

         if (effect == Effects.DOG_CAR)
             return 48;

         if (effect == Effects.FREEZE_GAME_RED_HELMET_GLOWING)
             return 49;

         if (effect == Effects.FREEZE_GAME_GREEN_HELMET_GLOWING)
             return 50;

         if (effect == Effects.FREEZE_GAME_WHITE_HELMET_GLOWING)
             return 51;

         if (effect == Effects.FREEZE_GAME_BLACK_HELMET_GLOWING)
             return 52;

         if (effect == Effects.BIRDIES_OVER_HEAD)
             return 53;

         if (effect == Effects.BUNNY_CAR)
             return 54;

         if (effect == Effects.BLUE_MALE_ROLLER_SKATES)
             return 55;

         if (effect == Effects.PINK_FEMALE_ROLLER_SKATES)
             return 56;

         if (effect == Effects.SLOWLY_GLOWING)
             return 57;

         if (effect == Effects.SLOWLY_GLOWING2)
             return 58;

         if (effect == Effects.PULSATING_GLOW)
             return 59;

         if (effect == Effects.CROCODILE_HEAD)
             return 60;

         if (effect == Effects.BIG_YELLOW_STARS_OVER_HEAD)
             return 61;

         if (effect == Effects.BIG_PURPLE_STARS_OVER_HEAD)
             return 62;

         if (effect == Effects.BIG_PINK_STARS_OVER_HEAD)
             return 63;

         if (effect == Effects.CANDLE)
             return 64;

         if (effect == Effects.MOBILE_PHONE_RINGING)
             return 65;

         if (effect == Effects.RED_GLOWING_WAND)
             return 66;

         if (effect == Effects.RIO_BIRD)
             return 67;

         if (effect == Effects.BUNNY_RUN)
             return 68;

         if (effect == Effects.RED_VROOM_CAR)
             return 69;

         if (effect == Effects.PANDA)
             return 70;

         if (effect == Effects.BLACK_SKATEBOARD)
             return 71;

         if (effect == Effects.BLACK_SKATEBOARD2)
             return 72;

         if (effect == Effects.RED_LOLLIPOP_HEAD)
             return 73;

         if (effect == Effects.COLOUR_FLOWERS_AROUND_NECK)
             return 74;

         if (effect == Effects.COLOUR_FLOWERS_AROUND_NECK2)
             return 75;

         if (effect == Effects.COLOUR_FLOWERS_AROUND_NECK3)
             return 76;

         if (effect == Effects.HORSE_SADDLE)
             return 77;

         if (effect == Effects.PERRI)
             return 78;

         if (effect == Effects.PURPLE_ALIEN)
             return 79;

         if (effect == Effects.HALLOWEEN_MOUTH_FACE)
             return 80;

         if (effect == Effects.HALLOWEEN_GREEN_HEAD)
             return 81;

         if (effect == Effects.HALLOWEEN_MASK)
             return 82;

         if (effect == Effects.HALLOWEEN_MASK2)
             return 83;

         if (effect == Effects.HALLOWEEN_FACE_MUMMY)
             return 84;

         if (effect == Effects.PUMPKIN_HEAD)
             return 85;

         if (effect == Effects.POTATO_SACK_MASK)
             return 86;

         if (effect == Effects.WAREWOLF_MASK)
             return 87;

         if (effect == Effects.HALLOWEEN_GREEN_MASK)
             return 88;

         if (effect == Effects.HALLOWEEN_KNIFE_THROUGH_BODY)
             return 89;

         if (effect == Effects.BUTTERFLY_WINGS)
             return 90;

         if (effect == Effects.GHOSTLY_BUTTERFLY_WINGS)
             return 91;

         if (effect == Effects.GHOST_COVER)
             return 92;
        
         return 0;
     }
}
